{{#unless isSecondary}}
    {{#foreach navigation}}
        <a class="menu-item menu-item-{{slug}}{{#if current}} menu-item-current{{/if}} {{link_class for=slug}}" href="{{url absolute="true"}}">{{label}}</a>
    {{/foreach}}
{{else}}
    {{#foreach navigation}}
        <a class="menu-item menu-item-{{slug}}{{#if current}} menu-item-current{{/if}}" href="{{url absolute="true"}}">{{label}}</a>
        <span class="sep"> ✦ </span>
    {{/foreach}}
{{/unless}}